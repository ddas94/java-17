server:
  port: 8080
  servlet:
    context-path: /java-17-test
  error:
    whitelabel:
      enabled: false
spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:test;MODE=Oracle;
    username: test
    password:

  jpa:
    generate-ddl: false
    hiberntae:
      ddl-auto: none